/* import { useEffect, useState } from "react";
import { getNotes, createNote, updateNote, deleteNoteApi } from "../services/noteApi";

export function useNotes() {
  const [notes, setNotes] = useState([]);
  const [input, setInput] = useState("");
  const [filter, setFilter] = useState("all");
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");

  // fetchNotes here
  // addNote here
  // deleteNote here
  // toggleNote here
  // editNote here
  // filteredNotes here

  return {
    notes,
    input,
    setInput,
    filter,
    setFilter,
    loading,
    error,
    filteredNotes,
    addNote,
    deleteNote,
    toggleNote,
    editNote,
  };
} */